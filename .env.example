# =============================================================================
# KARYAKARTA APP ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Controls whether payment functionality is enabled
# Set to "true" to enable payments, "false" to disable
NEXT_PUBLIC_PAYMENTS_ENABLED=false

# =============================================================================
# FIREBASE CONFIGURATION
# =============================================================================
# Client SDK configuration (for web Firebase client)
# These are safe to expose to the client
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id

# Admin SDK configuration (for server-side operations)
# These should be kept secret
FIREBASE_PROJECT_ID=your_project_id
GCLOUD_PROJECT=your_project_id

# =============================================================================
# FIREBASE EMULATOR CONFIGURATION (Development Only)
# =============================================================================
# Uncomment these lines to use Firebase emulators for local development
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
# FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099

# =============================================================================
# PAYMENT CONFIGURATION (Only required if NEXT_PUBLIC_PAYMENTS_ENABLED=true)
# =============================================================================
# Razorpay Test Keys (for development and testing)
# Get these from: https://dashboard.razorpay.com/#/app/keys
RAZORPAY_KEY_ID_TEST=rzp_test_your_test_key_id_here
RAZORPAY_KEY_SECRET_TEST=your_test_key_secret_here
NEXT_PUBLIC_RAZORPAY_KEY_TEST=rzp_test_your_test_key_id_here

# Razorpay Live Keys (for production)
# Get these from: https://dashboard.razorpay.com/#/app/keys
# RAZORPAY_KEY_ID_LIVE=rzp_live_your_live_key_id_here
# RAZORPAY_KEY_SECRET_LIVE=your_live_key_secret_here
# NEXT_PUBLIC_RAZORPAY_KEY_LIVE=rzp_live_your_live_key_id_here

# Razorpay Webhook Secret (for webhook verification)
# Get this from: https://dashboard.razorpay.com/#/app/webhooks
RAZORPAY_WEBHOOK_SECRET_TEST=your_webhook_secret_here
# RAZORPAY_WEBHOOK_SECRET_LIVE=your_live_webhook_secret_here

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Set to "1" to enable mock mode (bypasses real payment processing)
# USE_MOCK=1

# Set to "1" to enable mock mode for client-side operations
# NEXT_PUBLIC_USE_MOCK=1

# Base URL for the application (used for webhooks and notifications)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# =============================================================================
# NOTES
# =============================================================================
# 1. When NEXT_PUBLIC_PAYMENTS_ENABLED=false:
#    - All payment-related functionality is disabled
#    - Razorpay SDK is not loaded
#    - Users see "payments coming soon" message
#    - Bookings complete without payment
#
# 2. When NEXT_PUBLIC_PAYMENTS_ENABLED=true:
#    - Payment functionality is enabled
#    - Razorpay keys are required
#    - Build will fail if required keys are missing
#
# 3. Environment Variable Naming:
#    - NEXT_PUBLIC_* variables are exposed to the client
#    - Variables without NEXT_PUBLIC_ are server-side only
#    - Use TEST keys for development, LIVE keys for production
#
# 4. Security:
#    - Never commit .env.local to version control
#    - Use different keys for test and production environments
#    - Rotate keys regularly in production

# =============================================================================
# REVIEWER & TESTING CONFIGURATION
# =============================================================================
# Token for accessing reviewer information page
# Generate a secure random token for production use
NEXT_PUBLIC_REVIEWER_TOKEN=reviewer_token_2025_karyakarta

# Test credentials for reviewers and QA testing
# These are safe to expose as they're test accounts only
NEXT_PUBLIC_TEST_CUSTOMER_EMAIL=customer@test.com
NEXT_PUBLIC_TEST_CUSTOMER_PASSWORD=testpass123
NEXT_PUBLIC_TEST_ADMIN_EMAIL=admin@test.com
NEXT_PUBLIC_TEST_ADMIN_PASSWORD=adminpass123

# =============================================================================
# NOTES
# =============================================================================
# 1. Reviewer token should be changed for production deployments
# 2. Test credentials are for review purposes only
# 3. Access reviewer info at: /reviewer-info?token=YOUR_TOKEN
# 4. Reviewer page only visible in development mode

# =============================================================================
# ERROR TRACKING & MONITORING
# =============================================================================
# Sentry DSN for error tracking and performance monitoring
# Get this from: https://sentry.io/settings/projects/
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Sentry organization and project (for source maps)
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=your-sentry-project

# Sentry auth token (for uploading source maps)
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# =============================================================================
# NOTES
# =============================================================================
# 1. Error tracking is automatically enabled when Sentry DSN is configured
# 2. In development, errors are logged to console
# 3. In production, errors are sent to Sentry
# 4. Global error boundary catches all unhandled errors
# 5. Logger provides structured logging with context
